# payo-app


/////////////////////////////////

# Install biometrics package

npm install react-native-biometrics 

# Install async storage
npm install @react-native-async-storage/async-storage


# Install image picker
npm install react-native-image-picker

# For iOS, install pods
cd ios && pod install && cd ..

# If you're using React Native Vision Camera
npm install react-native-vision-camera
npm install react-native-permissions

# To install Redux Toolkit and React‑Redux, run one of the following commands in your project root:

npm install @reduxjs/toolkit react-redux

npm install react-redux react-native-worklets




npm install @react-navigation/native
npm install react-native-screens react-native-safe-area-context
npm install react-native-vector-icons



npm install react-native-curved-bottom-bar
npm install react-native-svg
npm install react-native-vector-icons


npm install react-native-webview

npm install @react-native-community/geolocation